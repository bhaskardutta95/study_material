/**
 * Site footer — shared by every page via PageLayout.
 */
export default function Footer() {
  return (
    <footer className="site-footer">
      <p>
        StudyDeck — a study companion built from course material. Content for
        revision only.
      </p>
    </footer>
  );
}
